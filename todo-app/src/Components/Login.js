import React, { useState } from "react";
import { Button } from "@material-ui/core";
import MainInfo from "./PageSeccions/LoginForm";

const Login = () => {
  return (
    <div style={{ background: "linear-gradient(to left, #af69cd, #4a148c)" }}>
      <MainInfo />
    </div>
  );
};

export default Login;
